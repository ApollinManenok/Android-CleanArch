<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.pmanenok.tasks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.screen.student.StudentActivity"></activity>
        <activity android:name=".presentation.screen.menu.MenuActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.screen.hw9.HW9Activity"></activity>
        <activity android:name=".presentation.screen.hw10.HW10Activity">
        </activity>
    </application>
</manifest>
